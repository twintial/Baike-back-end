<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.baike.mapper.CommentMapper">
    <insert id="insertComment">
        insert into BK_Comments(uID, interVideoID, content, sendTime)
        values (#{uID}, #{interVideoID}, #{content}, #{sendTime})
    </insert>
</mapper>
